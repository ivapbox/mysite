{% extends 'app/home.html.twig' %}

{% block content %}
    {% for product in products %}
        <h3><a href="{{ path('product_card', {'id' : product.id }) }}">{{ product.name }}</a></h3>
    {% endfor %}
{% endblock %}
